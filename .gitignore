node_modules
dist
dist-ssr
*.local
.vite
.DS_Store
.env
.env.local

# Real backend credentials — database password, Stripe secret key.
# Only config.example.php belongs in the repo.
server/config.php
eaton-config.php
server/logs/
*.log

# Rendered email previews, generated for eyeballing the templates.
email-preview/

# Deployment archives — rebuilt from source, no reason to version them.
*.zip

# The raw browser export. It can contain customer orders with names, phone
# numbers and addresses, so it stays out of git; only the generated
# server/local-data.sql and the image files are committed.
eaton-export.json
